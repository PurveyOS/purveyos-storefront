import type { StorefrontTemplateProps } from '../types/storefront';

export function HomePage(_props: StorefrontTemplateProps) {
  // This component is used when routing to "/" 
  // but in our case, StorefrontRoot handles template rendering
  return null;
}